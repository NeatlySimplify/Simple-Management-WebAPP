# AUTOGENERATED FROM 'app/queries/unit/updateUnit.edgeql' WITH:
#     $ edgedb-py


from __future__ import annotations
import dataclasses
import datetime
import edgedb
import uuid


class NoPydanticValidation:
    @classmethod
    def __get_pydantic_core_schema__(cls, _source_type, _handler):
        # Pydantic 2.x
        from pydantic_core.core_schema import any_schema
        return any_schema()

    @classmethod
    def __get_validators__(cls):
        # Pydantic 1.x
        from pydantic.dataclasses import dataclass as pydantic_dataclass
        _ = pydantic_dataclass(cls)
        cls.__pydantic_model__.__get_validators__ = lambda: []
        return []


@dataclasses.dataclass
class updateUnitResult(NoPydanticValidation):
    id: uuid.UUID


async def updateUnit(
    executor: edgedb.AsyncIOExecutor,
    *,
    email: str,
    name: str,
    relationship: str,
    govt_id: str,
    sex: str,
    details: str,
    type_unit: str,
    birth: datetime.date,
    custom_fields: str,
    id: uuid.UUID,
) -> updateUnitResult | None:
    return await executor.query_single(
        """\
        update Unit filter .id = <uuid>$id set {
            email := <str>$email ?? .email,
            name := <str>$name ?? .name,
            relationship := <str>$relationship ?? .relationship,
            govt_id := <str>$govt_id ?? .govt_id,
            sex := <str>$sex ?? .sex,
            details := <str>$details ?? .details,
            type_unit := <str>$type_unit ?? .type_unit,
            birth := <cal::local_date>$birth ?? .birth,
            custom_fields := <json>$custom_fields ?? .custom_fields,
        }\
        """,
        email=email,
        name=name,
        relationship=relationship,
        govt_id=govt_id,
        sex=sex,
        details=details,
        type_unit=type_unit,
        birth=birth,
        custom_fields=custom_fields,
        id=id,
    )
